Listy kontroli dosêpu (ACL) maj¹ za zadanie uniemo¿liwiæ komunikowanie siê pracowni komputerowych z komputerami znajduj¹cymi siê w sekretariacie. Na routerze R2 tworzona jest acces lista na której znajduje siê adres NAT pracowni komputerowych wraz z odwrócon¹ mask¹.

		Router# conf t
		Router(config)#acces-list 1 deny 193.0.3.1 0.0.0.255 	
		Router(config)# interface FastEthernet <interfejs do switcha>
		Router(config-if)#ip access-group 1 in	
		Router(config-if)#exit 